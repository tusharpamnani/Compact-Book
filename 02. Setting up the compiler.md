# Setting Up the Compiler

Compact has two tools:
- **`compact`**: the CLI you use day-to-day
- **`compactc`**: the compiler (invoked via `compact compile`)

> Source: [docs.midnight.network/getting-started/installation](https://docs.midnight.network/getting-started/installation)

## Prerequisites

- Google Chrome browser
- Visual Studio Code
- Docker Desktop
- Lace Wallet (Chrome extension)

> Supported: Linux, macOS. Use WSL on Windows.

## Step 1: Install the `compact` CLI

```bash
curl --proto '=https' --tlsv1.2 -LsSf https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh
```

Add to your PATH:
```bash
export PATH="$HOME/.compact/bin:$PATH"
```

## Step 2: Install the Compiler

```bash
compact update
```

Verify:
```bash
compact --version
compact compile --version
```

## Step 3: Start the Proof Server

The proof server generates ZK proofs locally. Requires Docker Desktop running:

```bash
docker run -p 6300:6300 midnightntwrk/proof-server:8.0.3 midnight-proof-server -v
```

The server listens at `http://localhost:6300`. Configure Lace Wallet at **Settings → Midnight** → select `Local (http://localhost:6300)`.

## Step 4: Install the VS Code Extension

1. Download the [VSIX package](https://raw.githubusercontent.com/midnight-ntwrk/releases/gh-pages/artifacts/vscode-extension/compact-0.2.13/compact-0.2.13.vsix)
2. In VS Code: **Extensions → Install from VSIX**

## Compiling a Contract

```bash
compact compile contracts/counter.compact contracts/managed/counter
```

Output:
```
contracts/managed/counter/
├── contract/             # Contract code
│   ├── index.js
│   ├── index.d.ts
│   └── index.js.map
├── compiler/             # Metadata
│   └── contract-info.json
├── zkir/                 # ZK circuits
│   ├── *.zkir
│   └── *.bzkir
└── keys/                 # Proving keys
    ├── *.prover
    └── *.verifier
```

Skip ZK key generation during development:
```bash
compact compile --skip-zk contracts/counter.compact contracts/managed/counter
```

## Other Commands

```bash
compact format src/          # format all .compact files
compact format --check src/  # check formatting (CI)
compact fixup src/           # apply source-level fixups
compact list --installed     # list installed versions
```

## Standard Library

Add at the top of your `.compact` file:

```compact
import CompactStandardLibrary;
```

This is built into the compiler, not a file on disk.

## Troubleshooting

| Error | Fix |
|-------|-----|
| `compact: command not found` | Add `$HOME/.compact/bin` to PATH |
| Docker connection errors | Ensure Docker Desktop is running |
| Port 6300 in use | Use different port: `-p 6301:6300` |